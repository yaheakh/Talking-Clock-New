# تفعيل الصوت المسجّل محليًا (يعمل حتى بدون محرك TTS في النظام)

## العمل في الخلفية (خارج التطبيق)
تم إضافة خدمة خلفية حقيقية (Foreground Service) تُبقي الساعة تنطق
بالوقت والتذكيرات حتى لو خرجت من التطبيق تمامًا — سواء بزر Home
بالريموت، أو بزر التصغير الجديد (⊖) أعلى الشاشة بجانب زر الإعدادات.
ستلاحظ إشعارًا صغيرًا ثابتًا في شريط الحالة باسم "الساعة الناطقة" طالما
الخدمة تعمل — هذا سلوك أندرويد القياسي لأي خدمة خلفية حقيقية، ولا يمكن
إخفاؤه بالكامل (وهو نفس السلوك الذي تراه مع أي تطبيق موسيقى يعمل
بالخلفية مثلاً).

**ملاحظة مهمة بخصوص أجهزة التلفاز الرخيصة/المُقفلة:** بعض هذه الأجهزة
تأتي بمدير بطارية خاص بالشركة المصنّعة يقتل أي خدمة خلفية بقوة، بغض
النظر عن أفضل الممارسات البرمجية. إن توقف الصوت في الخلفية رغم هذا
التحديث، تحقّق من إعدادات الجهاز نفسه في: البطارية > تحسين البطارية،
وأضف هذا التطبيق لقائمة الاستثناءات، أو فعّل "التشغيل التلقائي" له إن
وُجد هذا الخيار.

## الطريقة الأسهل: صفحة الويب (voice_generator.html)
افتح ملف `voice_generator.html` (موجود بجانب هذا الملف) في أي متصفح —
مباشرة من جهازك، لا حاجة لرفعه لأي مكان. ثم:
1. ضع مفتاح Google Cloud Text-to-Speech API الخاص بك.
2. اختر الصوت الإنجليزي والعربي المفضّلين.
3. اضغط "اختبار سريع" للتأكد من صحة المفتاح.
4. اضغط "توليد كل الأصوات وتنزيلها" — سيولّد الصفحة كل المقاطع ويحمّلها
   كملف `voice-assets.zip` تلقائيًا.
5. فُك ضغط الملف، وانسخ مجلد `assets` بالكامل إلى داخل مجلد
   `Talking-Clock-New-main` (بجانب `lib/` و`pubspec.yaml`).
6. ادفع (commit/push) مجلد `assets` الجديد إلى GitHub، وشغّل البناء
   كالمعتاد.

كل شيء يعمل من داخل المتصفح مباشرة — لا خادم وسيط، والمفتاح لا يُرسَل
لأي مكان سوى خوادم Google نفسها.

## إن كنت تبني عبر GitHub Actions بدون صفحة الويب
**لا تحتاج تفعل أي شيء يدويًا.** أضفتُ خطوات إلى ملف الأتمتة تُولّد ملفات
الصوت تلقائيًا في كل مرة تُشغَّل فيها عملية البناء على GitHub (تثبيت
ffmpeg وedge-tts، ثم تشغيل `generate_voice_clips.py` قبل `flutter build
apk`). فقط ادفع (push) هذه الملفات إلى مستودعك على GitHub كما هي، وشغّل
البناء من تبويب Actions — وستجد الصوت المسجّل جاهزًا داخل الـ APK الناتج.

## إن أردت تشغيل سكربت بايثون يدويًا على جهازك بدلًا من ذلك (اختياري)
```bash
pip install edge-tts
```
وتثبيت ffmpeg (مجاني):
- ويندوز: حمّله من https://ffmpeg.org/download.html أو `choco install ffmpeg`
- ماك: `brew install ffmpeg`
- لينكس: `sudo apt install ffmpeg`

ثم من داخل مجلد المشروع:
```bash
python3 generate_voice_clips.py
flutter pub get
flutter build apk
```

## ملاحظات
- إعلان الوقت الدوري (كل ساعة/دقيقة حسب إعداداتك) سيستخدم الآن هذه
  المقاطع تلقائيًا (يمكن إيقاف ذلك من: الإعدادات > الصوت > "استخدم الصوت
  المسجّل محليًا").
- عند إضافة تذكير جديد، يمكنك الآن الاختيار بين "قائمة جاهزة (صوت
  مضمون)" — عناوين شائعة (دواء، صلاة، اجتماع...) لها صوت مسجّل مسبقًا
  يعمل حتى بدون محرك نطق في النظام — أو "نص حر" كما كان سابقًا، والذي
  يبقى يحتاج محرك نطق النظام. التذكيرات الموجودة حاليًا في التطبيق تبقى
  كما هي (نص حر) دون أي تغيير.
- زر "تشخيص الصوت" الذي أُضيف سابقًا سيخبرك إن كان الجهاز يملك محركًا
  لنطق النصوص الحرة أم لا.
- إن أردت صوتًا مختلفًا، غيّر `VOICE_EN` و `VOICE_AR` في أعلى السكربت،
  ونفّذ `edge-tts --list-voices` لرؤية كل الأصوات المتاحة.
