<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Required on Android 14 (API 34)+ to run any foreground service at
         all — the service type below declares this one as media playback
         (it speaks the clock/reminders), and Android 14 hard-crashes any
         foreground service on launch if its type isn't declared both here
         and on the <service> tag below. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>

    <!-- required="false" on both so the SAME apk installs and runs fine
         on a regular phone/tablet (touchscreen, no leanback) AND on an
         Android TV (leanback, no touchscreen) -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:label="Talking Clock"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Some locked-down / OEM Android TV boxes (e.g. Ooredoo TV boxes)
             ship with old or buggy GPU drivers that don't render Flutter's
             default Impeller renderer at all — the result is the launch
             screen (logo) staying up forever, since the first real frame
             never draws. Forcing the older, far more broadly-compatible
             Skia renderer fixes this on that class of device without
             affecting normal Android TVs / phones, where Skia is still
             perfectly fast for an app this simple. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- Required by flutter_local_notifications for scheduled
             (zonedSchedule) notifications to keep firing, and to survive
             a device reboot — this is what lets a reminder still go off
             even if the phone/TV was restarted since the app last ran. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- The background service's own <service> element, from
             flutter_background_service. On Android 14+, the system
             throws immediately on launch (crashing the whole app) if a
             foreground service doesn't declare its type here AND request
             the matching permission above — this exact mismatch is what
             was closing the app right after the loading screen. -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false"
            tools:replace="android:exported" />
    </application>
</manifest>
